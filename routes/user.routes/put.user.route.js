import express from 'express'
const router = express.Router();

/* PUT update an existing user in database */
const updateUser = router.put('/', (req, res, next) => {
	
})

export default updateUser;
