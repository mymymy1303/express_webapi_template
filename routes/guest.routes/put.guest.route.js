import express from 'express'
const router = express.Router();

/* PUT update an existing guest in database */
const updateGuest = router.put('/', (req, res, next) => {
	
})

export default updateGuest;
