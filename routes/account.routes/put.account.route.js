import express from 'express'
const router = express.Router();

/* PUT update an existing account in database */
const updateAccount = router.put('/', (req, res, next) => {
	
})

export default updateAccount;
