import express from 'express';
const router = express.Router();

/* GET account by username */
const getSingleAccount = router.get('/:username', (req, res, next) => {

});

export default getSingleAccount;