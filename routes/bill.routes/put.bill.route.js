import express from 'express'
const router = express.Router();

/* PUT update an existing bill in database */
const updateBill = router.put('/', (req, res, next) => {
	
})

export default updateBill;
